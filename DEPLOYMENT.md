# 小白上线清单

这份清单按“先把第一版稳定放到网上”的方式写，默认使用 Vercel。

## 1. 先准备

- [ ] 网站在自己电脑上能正常打开
- [ ] 运行 `npm run build` 能成功
- [ ] 准备 GitHub 账号
- [ ] 准备 Vercel 账号
- [ ] 准备域名；没有域名也可以先用 Vercel 给的测试网址
- [ ] 准备高德地图的 `VITE_AMAP_KEY` 和 `VITE_AMAP_SECURITY_CODE`

## 2. 把代码放到 GitHub

1. 在 GitHub 新建一个仓库。
2. 把当前项目推送到这个仓库。
3. 后续每次改网站，都先把代码保存到 GitHub，再发布。

## 3. 在 Vercel 发布测试版

1. 登录 Vercel。
2. 选择导入刚才的 GitHub 仓库。
3. 确认项目类型是 Vite；一般会自动识别。
4. 在环境变量里添加：

   | 名称 | 填什么 |
   | --- | --- |
   | `VITE_AMAP_KEY` | 你的高德地图 Key |
   | `VITE_AMAP_SECURITY_CODE` | 你的高德地图安全密钥 |
   | `VITE_ENABLE_LOCAL_EDITING` | 正式站保持 `false`，不要开放案例和点位维护入口 |

5. 点击发布。
6. 打开 Vercel 生成的测试网址。

## 4. 测试网址验收

- [ ] 首页能打开
- [ ] 案例地图能打开
- [ ] 点击案例后能进入详情
- [ ] 场景演示能切换模式
- [ ] 图片、全景图和产品点位能显示
- [ ] 手机和电脑都检查一遍
- [ ] 直接打开 `/cases` 这类页面地址也能进入网站

## 5. 绑定正式域名

1. 在 Vercel 项目设置里添加你的域名。
2. 按 Vercel 给出的提示，到域名服务商那里完成解析。
3. 等域名生效后，确认正式网址能打开。
4. 确认浏览器地址是 `https://` 开头。

## 6. 上线后怎么改

每次调整都按这个顺序：

1. 在本地修改。
2. 需要用页面维护固定案例或产品点位时，在本地开发页面操作并导出配置。
3. 本地运行 `npm run build` 检查。
4. 推送到 GitHub。
5. 先看 Vercel 的测试预览。
6. 确认没问题后，再让正式站点更新。

正式站默认隐藏“编辑案例内容”和“管理模式”。页面里的本地编辑结果只保存在当前浏览器，不会直接改动线上内容；如果临时需要在预览站维护，再把 `VITE_ENABLE_LOCAL_EDITING` 设为 `true`，维护完成后正式站仍保持 `false`。

## 7. 以后再补

- [ ] 访问统计
- [ ] 优先压缩大图片，改善打开速度
- [ ] 页面速度优化
- [ ] 更多手机适配检查
- [ ] 如果案例越来越多，再考虑后台管理
